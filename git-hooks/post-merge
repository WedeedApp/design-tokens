#!/bin/sh
# Hook git post-merge : rebuild automatiquement après un pull

# Skip dans GitHub Actions (CI)
if [ -n "$CI" ]; then
  echo "ℹ️  Hook post-merge désactivé dans GitHub Actions"
  exit 0
fi

echo "🔄 Mise à jour détectée, rebuild des tokens..."
npm run build
